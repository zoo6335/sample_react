import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import Memo from './1018_hook/Memo';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Memo />
  </React.StrictMode>
);

// index.html을 연결해주는 파일로만 사용
